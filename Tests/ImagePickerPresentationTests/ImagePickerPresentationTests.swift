import XCTest
@testable import ImagePickerPresentation

final class ImagePickerPresentationTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ImagePickerPresentation().text, "Hello, World!")
    }
}
